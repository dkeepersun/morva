# Morva 下一阶段评估与行动建议

**核对时间：** 2026-08-10 　**事实基准：** commit `4a2a4ec`

本文是一次外部评估的结论与行动建议，不是承诺，也不是已批准规格。文中所有"当前行为"均由实际运行 CLI 与测试套件验证；覆盖率数字为量级估计，用于排序，不是可度量指标。

语言表达力的边界决策见 [语言演进决策](docs/language-evolution-policy.md)，本文不重复其内容，只引用。

---

## 一、结论摘要

**当前实现的不是"业务描述语言的 v0.1"，而是"单个操作契约验证器"的 v0.1。** 这个切片质量很高，但业务描述所需的其余维度大部分尚未开始。

三个判断：

| 问题 | 结论 |
|---|---|
| 工程质量是否达标？ | ✅ **是。** 门禁全绿，纪律罕见地严格 |
| 是否满足业务描述需求？ | ❌ **否。** 覆盖约三成，且缺口集中在最高频的表达上 |
| 是否具备 AI 接入条件？ | ❌ **否。** 零机器可读面，且当前存在会让 AI 产生"假成功"的行为 |

**最高优先级的一件事不是加功能，而是修掉一个正在把失败伪装成成功的行为**（见第四节 A-1）。

---

## 二、当前状态（实测）

| 项 | 实测值 |
|---|---|
| 源码 | core 4041 行 + CLI 874 行 |
| 测试 | 3158 行 / **134 个测试全绿** |
| 第三方依赖 | **0** |
| `cargo fmt --check` | ✅ PASS |
| `cargo clippy -D warnings` | ✅ PASS |
| 远程 CI | ✅ 已推送并成功运行 |
| branch protection | ❌ 未配置（前置条件已满足，可立即启用） |

已完成 8 个冻结规格：语义核心 → 最小模拟 → 静态表达式类型 → 有界诊断 → 通用换行 → 明显矛盾检测 → 多文件项目装配 → 可读源码注释。

质量门禁判定为 `CONCERNS`：P0 覆盖 100%，P1 覆盖 80%，唯一缺口是 NFR-04 性能无独立基准。

---

## 三、四项评估

### 3.1 作为业务描述语言的覆盖度

以下为实际写入 `.morva` 文件并执行 `check` 的结果：

| 业务表达 | 结果 |
|---|---|
| 总价 = 单价 + 运费 | ❌ 无算术 |
| VIP **或** 金额>100 免运费 | ❌ **无析取，且无任何 workaround** |
| VIP **且** 金额>100 | ✅ 多条子句隐式合取 |
| 折扣率 0.15 | ❌ 无 Decimal 字面量 |
| 金额 -1 | ❌ 无负数字面量 |
| 订单未关闭 `!order.open` | ❌ 无取反 |
| 用户名 = "bob" | ❌ 无 String 字面量 |
| 订单含多个订单项 | ❌ 无集合类型 |
| 下单 → 支付 → 发货 | ❌ scenario 硬限一个 `run` |
| 退款 ≤ 已付（跨实体） | ✅ |
| 1:1 关系字段穿透 | ✅ |
| 单操作前后置条件 + 七阶段模拟 | ✅ **强项** |

**合取有 workaround，析取没有。** 而"或"在业务规则中的密度极高——这是当前最硬的单点缺口。

维度盘点：

| 维度 | 状态 |
|---|---|
| 实体 / 字段 / 枚举 | ✅ 扎实 |
| 集合、1:N 关系、可选字段 | ❌ |
| 金额与数值（小数、负数、算术） | ⚠️ 类型可声明，值写不出来 |
| 文本字面量 | ❌ |
| 日期 / 时间 / 时长 | ❌ |
| 约束逻辑 AND | ✅ |
| 约束逻辑 **OR / NOT / 括号** | ❌ |
| 量词（每个 / 至少一个） | ❌ |
| 单操作前后置条件 | ✅ |
| 状态机 / 合法转移 | ❌ `lifecycle` 是空壳 |
| 多步流程编排 | ❌ |
| 单步模拟验证 | ✅ |
| 静态矛盾检测 | ✅ 保守但可用 |
| 多文件组织 | ✅ 已完成 |
| 模块作用域 / 限定名 | ❌ |

### 3.2 AI 接入就绪度

全仓库 `grep`：**无 MCP、无 JSON、无 serde、无 `--format`**。四个命令的输出全部是面向人眼的自由文本。

但缺的不是"生成能力"——AI 现在就能写 Morva，它是纯文本。缺的是让生成结果**可信、可迭代**的那一层。闭环 `生成 → check → 读诊断 → 修 → 再 check` 断在三处：

**断点 1：输出不可机读。** AI 必须正则 scrape，无法可靠提取 span。

**断点 2：AI 不知道边界，会产生语法幻觉。** 本次评估的 12 个探针文件由一个**已读完完整语言参考**的 AI 写出，仍一次性写出 8 种不存在的语法（`&&`、`||`、算术、`0.15`、`-1`、`!`、`"bob"`、`List<T>`）。这些不是随机错误，是从其他语言带来的**默认预期**。任何 LLM 都会犯，未读文档的会更糟。

**断点 3：兼容容器把失败伪装成成功。** 当 AI 想表达语言写不出的规则时，它有三条路：硬写幻觉语法（死循环烧 token）、塞进 `policy` 块（**`check` 返回 ok**）、悄悄省略规则。第二条最危险，因为它在所有环节都显示绿色——而 `policy` 恰好就在那里等着。

### 3.3 表达力边界

已形成正式决策，见 [语言演进决策](docs/language-evolution-policy.md)：

- 目标位置 **L2**（命题逻辑 + 线性算术），L3 冻结，L4 永久禁止；
- **非线性算术红线**（变量 × 变量）——由 Matiyasevich 定理，跨过即不可判定，不存在可用求解器；
- **`opaque` 逃生舱**——长尾规则通过"显式承认无知"承载，而非继续提升表达力；
- **保守近似原则**（无误报 / 允许漏报）使 L2 无需完备求解器，也无需引入 SMT 依赖。

### 3.4 工程质量与流程

强项：零依赖、门禁全绿、134 测试、冻结规格制度、可追溯性矩阵。**这套纪律比代码本身更有价值。**

两个风险：

**风险 1：文档漂移已经发生。** 5 处文档仍称"CI 尚未推送、尚无 hosted run"，而 CI 早已推送并成功运行过。一个把"不得声称远程 CI 已运行"写进冻结规格的项目，反过来在声称一件不成立的否定事实——说明"人工核对文档证据"这一流程本身不可靠。

**风险 2：`stable` 浮动工具链 + `clippy -D warnings` 是定时炸弹。** 无 `rust-toolchain.toml`、无 `rust-version`。每次 Rust 发布新 stable，新增 lint 会让门禁在无人改动代码的情况下变红。

---

## 四、行动清单

### A 组：立即可做，成本低，收益明确

**A-1　兼容容器显式化** ⭐ 最高优先级

现状：`policy` / `flow` / `lifecycle` / `service` / `event` / `module` 内可写任意垃圾内容，`check` 返回 `ok` 退出码 0，`inspect` 完全不提及，`parse` 只显示容器名。

这与"准确优先"原则（[requirements.md](docs/requirements.md) §4）直接冲突，且是 AI 接入的**阻断性问题**（3.2 断点 3）。

过渡措施：`check` 对每个兼容容器输出"已解析但无语义"提示，`inspect` 增加未建模声明清单，退出码保持 0。长期归宿见 [语言演进决策](docs/language-evolution-policy.md) §5.3——只能是"获得真实语义"或"归入 `opaque`"，不存在第三态。

**为什么排第一：** 不扩展任何语言能力，规格极小，却同时修复"人被误导"和"AI 被误导"两个问题。

**A-2　修正 5 处 CI 文档漂移**

并建议把可机检的声明变成 CI 检查项，而非依赖人工核对。

**A-3　启用 branch protection**

文档定的前置条件（workflow 远程成功运行至少一次）已满足。需在 GitHub 仓库设置中操作。

**A-4　钉住工具链版本**

添加 `rust-toolchain.toml` 与 `rust-version`（edition 2024 → MSRV ≥ 1.85）。可另开 `beta` 非阻塞 lane 提前发现新 lint。

**A-5　补 NFR-04 性能基准**

唯一让门禁停在 `CONCERNS` 的项。无需引入 criterion：生成 1x / 4x 规模模型，断言耗时比不超过设定上界即可。P1 覆盖 80% → 100%，门禁翻 `PASS`。

### B 组：语言表达力（需完整规格）

**B-1　L1 命题逻辑**（`||`、`!`、括号、嵌套谓词）

性价比最高的一步：表达力缺口最硬，可判定性零损失，且矛盾检测可从字面量启发式升级为对命题部分完备的判定。

估计规模与已完成的"明显矛盾检测"增量相当——parser 改优先级爬升约 150 行，checker 真值表穷举约 200 行。

**B-2　L2 线性算术 + 缺失字面量**（Decimal 小数、负数、String）

必须与 [演进决策 §4.2](docs/language-evolution-policy.md) 的非线性红线**同批落地**：红线需由 parser/checker 显式强制，配专属诊断码，且诊断消息必须说明"这是语言的永久边界，而非未实现的功能"——否则使用者与 AI 会反复尝试或等待。此项应写进 L2 规格的验收标准。

**B-3　`lifecycle` 状态机语义**（可与 B-1/B-2 并行考虑）

ROI 高的候选：状态流转是业务描述中出现频率最高的模式；关键字已在词法/语法中，落地路径短；不可达状态、缺失转移、非法转移全部可静态穷举，完全在"不做定理证明"的边界内；且能与现有前后置条件咬合——`effects order.status = Shipped` 可被校验为是否合法转移。

### C 组：AI 接入（依赖 B 组）

**C-1　`--format json`**

四个命令均需。诊断 JSON 最关键：`{code, message, file, line, col, span, severity}`。

架构决策：**core 手写序列化**（AST 类型有限，几百行足够且输出稳定性完全可控），第三方依赖隔离在下游 crate——零依赖原则得以保住。

**C-2　`morva-mcp` + 能力清单 + 修复导向诊断**

- 独立 crate 暴露 `check`/`parse`/`inspect`/`simulate` 为 MCP tool；
- 机器可读能力清单（支持的表达式形态、字面量种类、明确的"不支持"列表）作为 MCP resource 注入，用于压制 3.2 中实测的语法幻觉；
- 诊断增加 `help:` 段给出可行替代写法。这对 AI 修复成功率是数量级差异，对人也有用。

**边界约束：** 按"人在回路"原则（[requirements.md](docs/requirements.md) §4），MCP 工具集应为**只读 + 校验**，**不提供直接写 `.morva` 落盘的 tool**。生成结果须经人审核才成为事实。建议在设计阶段就写进边界，而非事后补。

### D 组：暂不建议

| 项 | 理由 |
|---|---|
| L3 集合与量词 | 成本为前面工作量总和量级；等真实模型反复撞墙的证据 |
| 多步 scenario | 有价值但需先明确多 action 间状态传递语义 |
| Tree-sitter / LSP | 依赖 C-1 的稳定机器表示 |
| grill / review / challenge | 依赖 B 组表达力与 C 组接入 |

---

## 五、一个战略提醒

[implementation-status.md](docs/implementation-status.md) 的"下一阶段候选"目前列的是 AI grill/review、Tree-sitter、LSP、图导出——**全部是工具链，没有一项是语言表达力**。

但从"满足业务描述需求"这个目标看，重心应该显式挪向 B 组。**工具链服务于一门表达力不足的语言，收益有限；而每补齐一块表达力，所有下游工具的价值都会跟着放大。**

同理，AI 接入（C 组）与表达力补齐（B 组）不是并行选项——B 是 C 的前置条件。若先建完管道而表达力不动，管道里流的将是持续撞墙的模型。

---

## 六、建议的推进顺序

```text
A-1 ─► A-2 A-3 A-4 A-5 ─► B-1 ─► B-2(+红线强制) ─► C-1 ─► C-2 ─► B-3 ─► D
最高优先   低成本清理         表达力          接入        视证据
```

A 组各项互相独立，可并行。B-1 应在 A-1 之后立项——A-1 会暴露出使用者实际把多少业务规则塞进了兼容容器，那正是 B 组优先级的真实证据。
