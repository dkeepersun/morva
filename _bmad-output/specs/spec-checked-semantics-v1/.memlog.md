---
subject: Morva Checked Semantics Protocol v1 production slice
slug: checked-semantics-v1
status: active
updated: 2026-08-14T00:03
---

- (direction) Express/headless distillation: production implementation specification plus the smallest single-file vertical slice; AI orchestration is excluded.
- (decision) CAP-1: a caller supplies one non-empty separator-free logical filename and exact UTF-8 source and receives a v1 checked-semantics document derived entirely by morva-core.
- (decision) CAP-2: a public core conformance seam rejects invalid envelopes, including wrong digest, invalid ranges/status/coverage/model references, before serialization.
- (decision) CAP-3: canonical serialization returns deterministic RFC 8259 UTF-8 JSON with specified field/capability order, two-space indentation, and final LF.
- (decision) CAP-4: repository-owned tests prove valid, invalid, warning-only, determinism, digest vectors, and negative conformance through public APIs without internal mocks.
- (decision) Public TDD seams confirmed from the user's accepted next-step contract: single-file producer and protocol-document validation/canonical serialization in morva-core; CLI is not in this slice.
- (direction) Protocol types are an explicit closed algebra, not direct serialization of AST/Rust internals, and no AI/review persistence/provider abstractions are introduced.
- (note) Existing architecture spine, protocol v1, schema, project context, architecture, requirements, and testing strategy remain adopted load-bearing companions.
- (gap) Human approval is required before changing the dependency-free morva-core contract; recommended decision is sha2 + serde derive + serde_json, with no default features where practical and locked versions via Cargo.lock.
- (assumption) [ASSUMPTION] SHA-256 remains the v1 revision algorithm only if dependency/change-control approval is granted; implementation cannot claim conformance before then.
- (event) Coherence pass: PASS; four capabilities each have intent/success, constraints bend design, non-goals and concrete success signal are present, one dependency question remains explicit.
- (event) Preservation pass: PASS; all load-bearing architecture/protocol/TDD/dependency-gate claims are in the kernel, implementation-contract companion, or adopted companions; workflow ceremony omitted.
- (event) SPEC.md and implementation-contract.md derived; companion paths, CAP-1..CAP-4 uniqueness, required kernel sections, and diff whitespace validated. Production TDD paused before first red test pending dependency approval.
