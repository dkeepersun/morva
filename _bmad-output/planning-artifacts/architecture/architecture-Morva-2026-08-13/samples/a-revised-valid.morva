system Fulfillment {
  enum OrderStatus {
    Pending
    Paid
    Fulfilled
    Cancelled
  }

  entity Order {
    id: ID
    status: OrderStatus
  }

  action Fulfill(order: Order) {
    requires order.status == Paid
    effects order.status = Fulfilled
    ensures order.status == Fulfilled
  }
}
