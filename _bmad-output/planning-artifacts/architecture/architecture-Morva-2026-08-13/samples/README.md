# AI validation samples

These samples were generated by an independent Codex agent on 2026-08-13. They are test evidence for the protocol design, not accepted Morva models.

- `a-*`: a diagnostic-preserving enum-member correction that human review can accept.
- `b-*`: an unsupported OR/NOT requirement; the revised candidate checks successfully but loses the `Failed` branch and must be rejected.
- `c-*`: warning-only compatibility/soft behavior; removing the warnings still leaves mandatory operational semantics unmodeled, so review remains blocked.

See `../VALIDATION-REPORT.md` for the exact prompts, observed CLI results, and verdicts.
